# b2b-car — 시작하기

법인 고객용 **탁송 접수·배차·정산 플랫폼**입니다. 고객이 웹이나 카카오 상담톡으로 오더를 넣으면
외부 배차 시스템 **콜마너**로 넘기고, 기사가 차를 옮기고, 사진·영수증을 받아 정산까지 갑니다.

전체 구조·DB·용어는 [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)에 있습니다. 이 문서는 **처음
30분 안에 알아야 할 것**만 담았습니다.

## 1. 띄우기

```bash
npm ci
cp .env.example .env      # 키 38개. 값은 팀에서 받으세요
npm run dev:all           # Express :3000 + next dev :3001
```

서버가 **두 개**입니다. Next(`:3001`)가 앞에 서고, 마이그레이션 플래그가 꺼진 경로는
Express(`:3000`)로 넘어갑니다. 코드를 고쳤으면 **둘 다** 재기동하세요 — 한쪽만 올리면
방금 고친 코드가 반영되지 않은 채 화면이 뜹니다.

> **로컬 DB가 운영 DB입니다.** 별도 개발 DB가 없습니다. 로컬에서 돌린 코드가 실제 고객에게
> 카카오톡을 보낼 수 있고, 오래된 개발서버를 켜둔 채 두면 실고객이 옛 코드의 응답을 받습니다.

## 2. 이 저장소가 일하는 방식

- **PR을 안 씁니다.** 작업은 `main`에 직푸시하고, Vercel이 곧바로 배포합니다(30~60초).
- 푸시하면 GitHub Actions의 **Verify**가 따로 돕니다(약 40초). 배포를 막지는 않고, 그 커밋이
  정말 프로덕션에 올라갔는지까지 확인해 줍니다.
- 커밋 전에 로컬에서 한 번 돌려주세요:

  ```bash
  npm run verify        # 문법 + EJS + 결정적 검사 40개 + Next 빌드
  ```

- **마이그레이션은 자동 적용되지 않습니다.** SQL은 사람이 Supabase에서 *먼저* 돌립니다.
  대기 목록은 `node scripts/check-migrations.js`.

## 3. 코드를 쓸 때 걸리는 것들

새로 온 사람이 거의 반드시 한 번은 밟습니다.

| 함정 | 실제로 무슨 일이 났나 |
|---|---|
| **오더 상태값이 한글** | `'completed'`·`'cancelled'`로 쓰면 조건이 아무것도 걸러내지 않고 조용히 통과합니다. `'완료'`·`'취소'`입니다 |
| **공용 UI가 두 벌** | `views/partials/*.ejs`만 고치면 부족합니다. `src/app/**/_components/`에 포팅된 쌍둥이가 있는지 같이 보세요. 고객용·관리자용도 갈립니다 |
| **접수 경로가 넷** | 웹 폼 / 웹 챗봇 / 카카오 상담톡 / 문의 전환. 하나만 고치면 셋이 빠집니다. 공통 로직은 `lib/orderCreate.js`에 두세요 |
| **oid는 서버가 덮어씀** | `createOrder()`에 `oid`를 넘겨도 `'OID' + (1000 + id)`로 바뀝니다 |
| **적요1은 100바이트** | 콜마너로 가는 기사 메모. 넘치면 말없이 잘리고 쓴 사람은 다 갔다고 믿습니다 |
| **새 컬럼은 `42703` 폴백** | 마이그레이션 전에 그 코드가 배포되면 화면이 통째로 죽습니다 |
| **응답 뒤 작업은 `runAfterResponse()`** | 서버리스는 응답을 보내면 얼어붙습니다. `lib/afterResponse.js` |
| **Next 16은 다릅니다** | Middleware가 `proxy.js`로 개명되는 등. 코드 쓰기 전에 `node_modules/next/dist/docs/`를 읽으세요 |

## 4. 테스트·자동화 규칙

운영 DB를 공유하기 때문에 규칙이 있습니다.

- **자동화는 QA 전용 계정만** 씁니다(`qa_test_*`). 로그인이 단일 세션이라 실사용 계정으로
  로그인하면 **그 계정을 쓰던 사람이 로그아웃됩니다.** 게이트가 시도 전에 막습니다.
- 시험 데이터는 존재하지 않는 도로명 **`검사로`**를 표식으로 씁니다. 검사·스펙은 끝에 자기 것을
  지우고, 죽었을 때를 대비해 `scripts/clean-test-orders.js`가 한 겹 더 있습니다(기본 미리보기,
  `--apply`로 실행).
- **범위 없는 `DELETE`를 쓰지 마세요.** 조건 없는 `DELETE FROM chat_sessions` 한 줄이 실고객
  상담톡까지 지우고 있었습니다.
- 위치로 지우지 마세요(예: "마지막 행 삭제"). 실데이터를 지울 수 있습니다.

## 5. 어디를 열어야 하나

| 하려는 일 | 볼 곳 |
|---|---|
| 도메인 로직 추가 | `lib/` (모듈 77개) — 새 기능은 대개 여기 |
| HTTP·권한 | `routes/` (32개) |
| Next 화면 | `src/app/**` · 분기는 `src/proxy.js` |
| EJS 화면 | `views/` |
| DB 질의 | `db.js` — `?` 플레이스홀더가 `$1`로 바뀝니다 |
| 요금 판정 | `lib/branchPolicy.js` (지사 설정 위에 법인 설정이 덮입니다) |
| 조회 범위 | `lib/clientScope.js` (본사 직원 vs 개인 딜러) |
| AI 호출 | `lib/vertexAi.js` — Edge Runtime에서는 못 돕니다 |
| 콜마너 연동 | `lib/callmaner.js` · 스펙은 `docs/callmaner-external-api-*` |
| 검사 추가 | `scripts/check-*.js` — CI에 넣으려면 `scripts/ci-verify.js`의 목록에 이름을 더하세요 |

## 6. 주석에 대해

이 저장소의 주석은 **왜 이렇게 됐는지**를 적습니다 — 사고가 났던 날짜와 증상까지 함께.
코드가 이상해 보이면 대개 그 위에 이유가 적혀 있습니다. 지우지 말고 읽어주세요.
새로 쓸 때도 같은 방식으로 남겨주시면 다음 사람이 같은 실수를 안 합니다.
