<%- include('../partials/header') %>
<div class="page-head-row">
  <div>
    <h1 class="page-title"><%= group.name %> · 탁송 요금</h1>
    <p class="page-sub">거리 기반 자동요금 계산 규칙입니다. 계산식: 기본요금 + (거리 − 기준거리) × (할증요금 ÷ 할증단위)</p>
  </div>
  <div class="page-head-actions">
    <a class="btn secondary" href="/groups">법인 목록으로</a>
    <button class="btn" type="submit" form="fareForm">저장</button>
  </div>
</div>
<%- include('../partials/group_tabs', { group: group, groups: groups, active: 'fare' }) %>

<% if (error) { %><div class="alert error"><%= error %></div><% } %>

<%# ── 지금 무엇으로 요금이 나오는가 ────────────────────────────────────────
    예전에는 이 카드가 지점 구간요금 아래에 있었고, 내용도 **거리 구간표만** 말했다. 그래서
    지점 구간요금을 켜둔 법인에서도 "이 법인 전용 요금표가 적용됩니다(구간 2개)"라고만 나와,
    실제로 먼저 적용되는 표가 무엇인지 알 수 없었다(사용자 지적 2026-08-29).

    두 표는 배타적이지 않다 — 지점 구간요금은 **걸릴 때만** 적용되고, 안 걸리는 구간은 거리
    구간표로 떨어진다. 그래서 하나를 고르는 식이 아니라 순위로 보여준다.

    화면 맨 위에 둔다: "지금 무엇이 적용되나"는 세부 설정보다 먼저 답해야 하는 질문이다. %>
<%
  const officeOn = group.office_fare_enabled !== false;
  const officeActive = officeOn && officeCount > 0;
  // 거리 구간표는 법인 표 → 지사 표 순으로 떨어진다(lib/branchPolicy.js loadFareTable).
  const distanceSource = tiers.length ? 'group' : (branchTiers.length ? 'branch' : 'none');
  const branchUsable = branchExtra.fare_table_enabled && branchTiers.length;
  // 둘 다 없으면 자동 계산이 아예 안 된다 — 이건 경고로 띄워야 한다.
  const noneUsable = !officeActive && distanceSource === 'none';
%>
<div class="card" style="border-left:4px solid #15803d;">
  <div class="section-title">📌 현재 적용되는 요금표</div>
  <p class="page-sub" style="margin-top:-4px;">
    위에서부터 순서대로 확인합니다. <b>앞 순위에서 걸리면 뒤는 보지 않습니다.</b>
  </p>

  <div class="table-wrap">
    <table class="table">
      <thead><tr><th style="width:70px;">순위</th><th style="width:190px;">요금표</th><th style="width:120px;">상태</th><th>내용</th></tr></thead>
      <tbody>
        <tr>
          <td><b>1</b></td>
          <td>지점 구간요금</td>
          <td>
            <% if (officeActive) { %><span class="badge green">적용 중</span>
            <% } else if (officeOn && !officeCount) { %><span class="badge gray">등록 없음</span>
            <% } else if (!officeOn && officeCount) { %><span class="badge amber">적용 꺼짐</span>
            <% } else { %><span class="badge gray">사용 안 함</span><% } %>
          </td>
          <td>
            <% if (officeActive) { %>
              지점 <b><%= officeCount %>곳</b> · 요금 <b><%= officeZoneCount.toLocaleString('ko-KR') %>줄</b> —
              출발지나 도착지가 등록 지점이면 이 금액으로 청구합니다.
            <% } else if (officeOn && !officeCount) { %>
              켜져 있지만 <b>등록된 지점이 없어</b> 적용되지 않습니다.
            <% } else if (!officeOn && officeCount) { %>
              지점 <%= officeCount %>곳이 등록돼 있으나 <b>적용을 꺼두었습니다.</b>
            <% } else { %>
              등록된 지점이 없고 적용도 꺼져 있습니다.
            <% } %>
          </td>
        </tr>
        <tr>
          <td><b>2</b></td>
          <td>거리 구간요금</td>
          <td>
            <% if (distanceSource === 'group') { %><span class="badge green">법인 전용</span>
            <% } else if (distanceSource === 'branch') { %><span class="badge blue">지사 표</span>
            <% } else { %><span class="badge red">없음</span><% } %>
          </td>
          <td>
            <% if (distanceSource === 'group') { %>
              이 법인 전용 요금표 (구간 <b><%= tiers.length %>개</b>)
              <% if (!extra.fare_table_enabled) { %>
                <%# 표가 있어도 "이 요금표 사용"이 꺼져 있으면 지사 표로 떨어진다. %>
                <span class="badge amber">이 요금표 사용 꺼짐</span> — 실제로는 지사 표로 계산합니다.
              <% } %>
            <% } else if (distanceSource === 'branch') { %>
              이 법인에 등록된 표가 없어 <b>소속 지사(<%= group.branch_name || '-' %>)</b> 표로 계산합니다.
              (지사 구간 <%= branchTiers.length %>개<%= branchExtra.fare_table_enabled ? '' : ' · 지사 요금표 사용 꺼짐' %>)
            <% } else { %>
              법인·지사 어디에도 거리 구간표가 없습니다.
            <% } %>
          </td>
        </tr>
      </tbody>
    </table>
  </div>

  <% if (noneUsable) { %>
    <div class="alert error" style="margin:10px 0 0;">
      <b>자동 요금 계산이 되지 않는 상태입니다.</b>
      지점 구간요금도 거리 구간표도 쓸 수 없어, 접수할 때 금액을 손으로 입력해야 합니다.
    </div>
  <% } else if (officeActive && distanceSource === 'none') { %>
    <div class="alert" style="margin:10px 0 0;background:#fef3e2;color:#b45309;border-color:#f5d9a8;">
      지점 구간요금에 <b>걸리지 않는 구간</b>은 계산할 표가 없습니다 — 그 경우 금액을 손으로 입력해야 합니다.
    </div>
  <% } %>

  <% if (distanceSource === 'branch' && branchUsable) { %>
    <form method="POST" action="/groups/<%= group.id %>/fare-rules/copy" style="margin-top:10px;"
          onsubmit="return confirm('소속 지사 요금표를 이 법인으로 복사할까요?');">
      <button class="btn secondary" type="submit">소속 지사 요금표 가져오기</button>
    </form>
  <% } %>
</div>

<%# ── 지점 구간요금 ────────────────────────────────────────────────────────
    거리 구간표보다 **먼저** 적용되므로 화면 제일 위에 둔다(사용자 지시). 아래에 있으면
    구간표만 고쳐놓고 "왜 요금이 안 바뀌지"를 겪는다.
    등록 화면은 따로 있지만(줄이 수백 개라) 켜고 끄는 것은 여기서 한다 — 요금이 어떻게
    산출되는지는 한 화면에서 보여야 한다. %>
<div class="card" style="border-left:4px solid #2e5c8a;">
  <div class="section-title">🏢 지점 구간요금 <span class="page-sub">(거리 구간표보다 먼저 적용)</span></div>
  <p class="page-sub">
    지점과 지역 사이의 <b>고정 요금표</b>입니다. 출발지 또는 도착지가 등록한 지점이면
    반대편 지역의 요금을 그대로 청구하고, <b>아래 거리 구간표는 건너뜁니다.</b>
  </p>

  <div class="row" style="align-items:center;gap:16px;">
    <div class="field" style="margin:0;">
      <%# 숨은 '0'과 함께 보낸다 — 체크박스는 켜졌을 때만 올라와서, 이게 없으면 끄는 것이 저장되지 않는다. %>
      <input type="hidden" name="office_fare_enabled" value="0" form="fareForm">
      <label class="checkline inline">
        <input type="checkbox" name="office_fare_enabled" value="1" form="fareForm"
               <%= group.office_fare_enabled === false ? '' : 'checked' %>>
        <b>지점 구간요금 우선 적용</b>
      </label>
    </div>
    <div class="field" style="margin:0;">
      <a class="btn secondary" href="/groups/<%= group.id %>/office-fares">지점 구간요금 등록 · 관리</a>
    </div>
  </div>

  <p class="page-sub" style="margin-bottom:0;">
    <% if (officeCount) { %>
      현재 <b>지점 <%= officeCount %>곳 · 요금 <%= officeZoneCount.toLocaleString('ko-KR') %>줄</b>이 등록돼 있습니다.
      <% if (group.office_fare_enabled === false) { %>
        <%# 등록해두고 꺼둔 상태는 헷갈린다 — 표가 있는데 안 쓰이고 있다는 걸 분명히 말한다. %>
        <span class="badge amber">적용 꺼짐</span> 지금은 이 표를 쓰지 않고 아래 거리 구간표로 계산합니다.
      <% } %>
    <% } else { %>
      등록된 지점이 없습니다. 체크되어 있어도 <b>적용되지 않고</b> 아래 거리 구간표로 계산합니다.
    <% } %>
  </p>
</div>

<form id="fareForm" method="POST" action="/groups/<%= group.id %>/fare-rules">
  <div class="card">
    <div class="section-title">📏 거리 구간별 요금 규칙</div>
    <%# 지점 구간요금이 이 표를 덮는다. 그 사실을 여기서 밝히지 않으면 관리자는 이 표를
        고쳐놓고 "왜 안 바뀌지"를 겪는다 — 요금이 안 맞는 원인을 찾을 곳이 다른 화면이다. %>
    <p class="page-sub" style="margin-top:0;">
      위 <b>지점 구간요금</b>에 해당하지 않는 오더에 이 표가 쓰입니다.
    </p>
    <div class="table-wrap">
    <table id="fareTiersTable">
      <thead>
        <tr>
          <th>구간</th><th>기준거리(km)</th><th>기본요금(원)</th><th>할증단위(km)</th><th>할증요금(원)</th>
          <th>최대거리(km)</th><th>최대요금(원)</th><th>반올림단위</th><th>반올림방식</th><th></th>
        </tr>
      </thead>
      <tbody id="fareTiersBody">
        <% tiers.forEach(function(t, i){ %>
          <tr>
            <td class="tier-label">구간요금<%= i + 1 %></td>
            <td><input type="number" name="base_distance_km" value="<%= t.base_distance_km %>" min="0" step="0.1"></td>
            <td><input type="number" name="base_fare" value="<%= t.base_fare %>" min="0" step="1000"></td>
            <td><input type="number" name="surcharge_unit_km" value="<%= t.surcharge_unit_km %>" min="0.1" step="0.1"></td>
            <td><input type="number" name="surcharge_fare" value="<%= t.surcharge_fare %>" min="0" step="100"></td>
            <td><input type="number" name="max_distance_km" value="<%= t.max_distance_km || '' %>" min="0" step="0.1"></td>
            <td><input type="number" name="max_fare" value="<%= t.max_fare || '' %>" min="0" step="1000"></td>
            <td><input type="number" name="round_unit" value="<%= t.round_unit %>" min="1" step="1"></td>
            <td>
              <select name="round_method">
                <option value="up" <%= t.round_method === 'up' ? 'selected' : '' %>>올림</option>
                <option value="round" <%= t.round_method === 'round' ? 'selected' : '' %>>반올림</option>
                <option value="down" <%= t.round_method === 'down' ? 'selected' : '' %>>내림</option>
              </select>
            </td>
            <td><button type="button" class="btn small secondary remove-tier-btn">삭제</button></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    </div>
    <button type="button" class="btn small secondary" id="addTierBtn" style="margin-top:10px;">+ 구간 추가</button>
  </div>

  <div class="card">
    <div class="section-title">➕ 부가 요금 / 노출 설정</div>
    <div class="row">
      <div class="field"><label>왕복 비율(%, 편도 대비)</label><input type="number" name="round_trip_ratio" value="<%= extra.round_trip_ratio || 180 %>" min="0"></div>
      <div class="field"><label>대기 기준시간(분)</label><input type="number" name="wait_threshold_min" value="<%= extra.wait_threshold_min || 15 %>" min="0"></div>
      <div class="field"><label>대기요금(원)</label><input type="number" name="wait_fee" value="<%= extra.wait_fee || 0 %>" min="0"></div>
    </div>
    <div class="row">
      <%# 취소요금은 1,000원 단위로 받는다(사용자 확정) — 지사 화면(views/branches/fare_rules.ejs)도 같다. %>
      <div class="field"><label>도착 전 취소요금(원)</label><input type="number" name="cancel_before_fee" value="<%= extra.cancel_before_fee || 0 %>" min="0" step="1000"></div>
      <div class="field"><label>도착 후 취소요금(원)</label><input type="number" name="cancel_after_fee" value="<%= extra.cancel_after_fee || 0 %>" min="0" step="1000"></div>
    </div>
    <%# 오지요금은 다른 할증들과 함께 아래 "탁송 특화 할증" 카드로 옮겼다 —
        같은 성격(구간요금에 더하는 정액)인데 따로 두면 상·하한 규칙이 갈린다. %>
    <div class="row">
      <div class="field"><label class="checkline"><input type="checkbox" name="fare_table_enabled" value="1" <%= extra.fare_table_enabled ? 'checked' : '' %>> 이 요금표 사용 (끄면 소속 지사 요금표를 씁니다)</label></div>
      <div class="field"><label class="checkline"><input type="checkbox" name="fare_visible_to_client" value="1" <%= extra.fare_visible_to_client !== 0 ? 'checked' : '' %>> 고객사(딜러)에게 요금 노출</label></div>
      <div class="field"><label class="checkline"><input type="checkbox" name="fare_editable_by_client" value="1" <%= extra.fare_editable_by_client ? 'checked' : '' %>> 고객사(딜러)의 금액 수정 허용</label></div>
    </div>
  </div>

  <%- include('../partials/fare_surcharge_settings') %>
</form>

<% if (saved) { %><div class="toast">저장되었습니다.</div><% } %>
<% if (copied) { %><div class="toast">소속 지사 요금표를 가져왔습니다.</div><% } %>
<script src="/js/fare-rules.js"></script>
<%- include('../partials/footer') %>
