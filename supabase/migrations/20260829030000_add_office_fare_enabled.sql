-- 지점 구간요금을 실제로 적용할지 — 법인별 스위치.
--
-- 지점 구간요금은 거리 구간표보다 먼저 적용된다. 그런데 표를 등록해두고도 "이번 달은 거리로
-- 계산하자"처럼 잠시 끄고 싶을 때가 있고, 지금은 끌 방법이 없다 — 줄을 다 지워야 한다.
--
-- 기본값은 true다. 이미 등록해둔 법인(코오롱모빌리티 1,176줄)의 동작이 마이그레이션만으로
-- 바뀌면 안 된다. 표가 없는 법인은 어차피 매칭이 안 되므로 true여도 영향이 없다.
alter table groups_tbl add column if not exists office_fare_enabled boolean default true;
