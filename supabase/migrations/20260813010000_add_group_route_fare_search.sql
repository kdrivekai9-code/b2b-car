-- 경로탐색(거리/소요시간/톨비)·요금검색(구간요금 계산)을 법인별로 켜고 끌 수 있게 한다
-- (실사용 요청: 이 결과를 실제로 안 보는 법인 때문에 접수 확인이 늦어질 필요가 없다).
--
-- 기본값은 true다 — 웹 AI 챗봇은 이미 예전부터 항상 경로/요금을 계산해 보여주고 있었으므로,
-- 기본을 꺼두면 기존 법인 전부가 하루아침에 그 안내를 못 받게 된다(회귀). 카카오톡 상담은
-- 이번에 새로 생기는 기능이라 위험이 작아 같은 기본값(켜짐)으로 시작해도 무리가 없다.
-- 굳이 끄고 싶은 법인만 관리자가 법인 관리 화면에서 끈다.
alter table groups_tbl add column if not exists route_fare_search_enabled boolean not null default true;
